apiVersion: v1
kind: Service
metadata:
  name: aminata-lofall-service
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    app: aminata-lofall-app