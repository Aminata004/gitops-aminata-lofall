replicaCount: 2

image:
  repository: 192.168.1.15:80/aminata-lofall/aminata-lofall-app
  tag: latest
  pullPolicy: Always

# Permet à Kubernetes de s'authentifier auprès du registre Harbor privé
imagePullSecrets:
  - name: harbor-secret

namespace: aminata-lofall

service:
  type: ClusterIP
  port: 80
  targetPort: 5000